apiVersion: v1
kind: Service
metadata:
  name: wallet-service
  namespace: default
  labels:
    app: wallet-service
spec:
  type: ClusterIP
  selector:
    app: wallet-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
