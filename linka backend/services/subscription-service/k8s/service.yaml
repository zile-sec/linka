apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  namespace: default
  labels:
    app: subscription-service
spec:
  type: ClusterIP
  selector:
    app: subscription-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
